# WORK LOG — 2026-04-25 (43차 보안 리뷰)

브랜치: `claude/review-codebase-Tui7S`
커밋: `03be088` (43차-1) → `3591293` (43차-2) → `9956677` (43차-3)

이 문서는 코드 / 코워크 / 모바일 / 온라인 동시 작업 시 충돌·중복 작업 방지를 위한
스냅샷이다. 각 작업은 이미 push 완료. 다음 작업자는 본 문서의 "남은 작업" 섹션과
"실행 대기 SQL" 섹션을 우선 확인할 것.

---

## 요약표

| 차수 | 작업 | 파일 | 상태 |
|------|------|------|------|
| 43차-1 | 네이버 세션 쿠키 KEK 암호화 (워커+API) | worker/src/lib/crypto.ts, worker/src/lib/credentials.ts, worker/src/adapters/naver.ts, app/api/platform-accounts/cookies/route.ts | ✅ 완료 (03be088) |
| 43차-1 | community 테이블 RLS 활성화 | supabase/migrations/20260425_community_rls.sql, supabase/community_points.sql | ✅ 코드 / ⏳ SQL 실행 대기 |
| 43차-1 | 관리자 화이트리스트 단일화 + 미들웨어 enforce | app/lib/admin-emails.ts, app/lib/adminAuth.ts, app/admin/layout.tsx, middleware.ts | ✅ 완료 |
| 43차-2 | AI 6 라우트 Redis 기반 레이트 리밋 | app/lib/rate-limit.ts, /api/ai, /api/ai-review-reply, /api/naver-blog-post, /api/video-script, /api/marketing/card-news/generate, /api/inquiry | ✅ 완료 (3591293) |
| 43차-2 | inquiry in-memory → Supabase 이관 | app/api/inquiry/route.ts, supabase/migrations/20260425_inquiries.sql | ✅ 코드 / ⏳ SQL 실행 대기 |
| 43차-2 | 워커 안정성: runJob try/catch + /health Redis ping + shutdown 타임아웃 + 오타 정리 | worker/src/index.ts | ✅ 완료 |
| 43차-3 | 3 cron 라우트 Bearer timing-safe 비교 (공통 헬퍼) | app/lib/cron-auth.ts, /api/cron/blog-tracking-daily, /api/cron/blog-tracking-digest, /api/cron/naver-reviews-fetch | ✅ 완료 (9956677) |
| 43차-3 | /api/worker-status 관리자 보호 | app/api/worker-status/route.ts | ✅ 완료 |
| 43차-3 | /api/naver-place 입력 길이·호스트 화이트리스트 검증 | app/api/naver-place/route.ts | ✅ 완료 |
| 43차-3 | /api/admin/crypto-selftest 응답에서 KEK 길이 제거 | app/api/admin/crypto-selftest/route.ts | ✅ 완료 |
| 43차-3 | review-reply 4 라우트 빈 catch → invalid_json 400 | bulk-draft, draft, submit, collect | ✅ 완료 |
| 43차-3 | 워커 KEK 모듈레벨 캐시 (재파싱 제거) | worker/src/lib/crypto.ts | ✅ 완료 |
| 43차-3 | 워커 diagnostics HTML 덤프 3000→800 + password value 마스킹 | worker/src/lib/diagnostics.ts | ✅ 완료 |
| 43차-3 | fly.toml `Mautty` → `memory` 오타 | worker/fly.toml | ✅ 완료 |
| 43차-3 | playwright 1.47.0 → ^1.59.0 (Dockerfile 일치) | worker/package.json | ✅ 완료 |
| 43차-3 | reviews.updated_at 컬럼 + 트리거 | supabase/migrations/20260425_reviews_updated_at.sql | ⏳ SQL 실행 대기 |
| 43차-3 | 4개 사용자 범위 뷰 security_invoker=true | supabase/migrations/20260425_view_security_invoker.sql | ⏳ SQL 실행 대기 |

---

## 실행 대기 SQL (Supabase SQL Editor 에서 한 번에)

> **순서대로 실행**. 각 파일은 idempotent 하므로 재실행해도 안전.
> 실행 후 결과(✅/오류 메시지)를 본 문서 `## SQL 실행 결과` 섹션에 추가할 것.

### 1) 20260425_community_rls.sql — community 테이블 RLS
```
경로: supabase/migrations/20260425_community_rls.sql
영향: community_posts / community_comments / community_post_likes / point_ledger
효과: anon/authenticated 직접 쓰기 차단 (service role 만 통과)
검증:
  SELECT relname, relrowsecurity FROM pg_class
   WHERE relname IN ('community_posts','community_comments','community_post_likes','point_ledger');
  → 모두 relrowsecurity = true
```

### 2) 20260425_inquiries.sql — 문의 영속 저장 테이블
```
경로: supabase/migrations/20260425_inquiries.sql
영향: public.inquiries 신규 생성 (서비스 롤 전용 RLS)
효과: 콜드스타트마다 /api/inquiry 데이터가 사라지던 문제 해결
검증:
  SELECT count(*) FROM public.inquiries;  -- 0 rows 기대 (또는 신규 문의)
  SELECT relrowsecurity FROM pg_class WHERE relname='inquiries'; -- true
```

### 3) 20260425_view_security_invoker.sql — 사용자 범위 뷰 보호
```
경로: supabase/migrations/20260425_view_security_invoker.sql
영향: my_customers / my_entitlements / place_snapshots_latest / place_targets_with_latest
효과: 베이스 테이블 RLS 가 호출자 권한으로 적용 (definer 우회 차단)
검증:
  SELECT relname, reloptions FROM pg_class
   WHERE relkind = 'v'
     AND relnamespace = 'public'::regnamespace
     AND relname IN ('my_customers','my_entitlements','place_snapshots_latest','place_targets_with_latest');
  → reloptions 에 'security_invoker=true' 포함
```

### 4) 20260425_reviews_updated_at.sql — reviews.updated_at 추가
```
경로: supabase/migrations/20260425_reviews_updated_at.sql
영향: public.reviews 에 updated_at timestamptz NOT NULL DEFAULT now() + 트리거
효과: 답글 수정 시각 추적 가능
검증:
  SELECT column_name, data_type FROM information_schema.columns
   WHERE table_schema='public' AND table_name='reviews' AND column_name='updated_at';
  SELECT tgname FROM pg_trigger WHERE tgrelid='public.reviews'::regclass;
  → trg_reviews_updated_at 존재
```

---

## 코드 사이드 배포 체크

| 항목 | 어디서 | 무엇 |
|---|---|---|
| 환경변수 ENCRYPTION_KEK_HEX | Vercel + Fly.io 동일 값 | 이미 설정되어 있어야 함 (변경 없음) |
| 환경변수 REDIS_URL | Vercel | 레이트 리밋이 동작하려면 필수. 미설정이면 fail-open (allow-all) |
| Vercel 재배포 | main → 프로덕션 | 43차-1·2·3 코드 반영 |
| Fly.io 워커 재배포 | localution-worker-kr | playwright ^1.59.0 + fly.toml memory 수정 + crypto/diagnostics 변경 |
| package-lock 재생성 | worker/ | `npm install` 실행해 lock 갱신 (Dockerfile 빌드 안정화) |

---

## 평문 쿠키 백필 (선택 — 1회성)

기존 row 의 `extra_data.session_cookies`(평문)는 다음 중 하나로 정리:

- 자동: 사용자가 다음 번 쿠키 재등록 또는 폼 로그인 성공 시 자동 암호화
- 수동: 아래 쿼리로 강제 클리어 후 재등록 유도
  ```
  UPDATE public.platform_credentials
     SET extra_data = extra_data - 'session_cookies'
   WHERE extra_data ? 'session_cookies'
     AND NOT extra_data ? 'session_cookies_encrypted';
  ```

---

## 의도적으로 남긴 항목 (별도 PR 권장)

| 항목 | 사유 |
|---|---|
| 토스 결제 시크릿 하드코딩 제거 / 토스 키 KEK 암호화 | 토스 키 발급 후 진행 (사용자 보류) |
| `subscriptions` UNIQUE 정책 | partial index IMMUTABLE 위반 우회 함수 필요. 영향 범위 큼 |
| `place_targets.user_id` text → uuid | 데이터 타입 변환은 destructive. 별도 백필 스크립트 필요 |
| `draft_reply` 큐 row-level lock | 워커 픽 쿼리(`UPDATE ... RETURNING`) 패턴 재설계 필요 |
| `browserSingleton` 동시성 보호 | 컨텍스트 풀링 재설계 — 운영 영향 큼 |
| `userAuth.ts` 토큰 파서 단순화 | 기능 변경 없는 리팩터, 회귀 위험 vs 가치 낮음 |

---

## SQL 실행 결과 (실행 후 채워넣기)

| # | 파일 | 실행자 | 일시 | 결과 |
|---|------|-------|------|------|
| 1 | 20260425_community_rls.sql | | | |
| 2 | 20260425_inquiries.sql | | | |
| 3 | 20260425_view_security_invoker.sql | | | |
| 4 | 20260425_reviews_updated_at.sql | | | |
