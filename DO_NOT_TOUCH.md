# DO NOT TOUCH — 사장님이 명시적으로 결정한 것들

> **AI / 자동 스크립트 / 리팩토링 작업 시 반드시 먼저 이 문서를 확인하세요.**
> 여기 적힌 항목은 사장님이 의도적으로 제거 / 변경한 결과이며, 다시 되돌리지 마세요.
> 새로운 기능을 추가할 때도 이 리스트와 충돌하지 않는지 확인하세요.

---

## 왜 이 문서가 필요한가 (Root cause)

대규모 자동화 작업 (이모지 일괄 제거 / 래퍼 표준화 / 새 기능 일괄 추가) 을 할 때 다음과 같은 문제가 반복됨:

1. **이전에 명시적으로 제거된 컴포넌트가 다시 추가됨**
   - 예: `QuickNav` (사이드바 옆 빠른 이동 위젯) — CLAUDE.md에 "제거됨" 명시했지만 다시 살아남
   - 원인: 자체 진행 작업 중 "사장님이 자주 쓰는 빠른 액션" 같은 비슷한 컴포넌트를 새로 만들면서 옛 컴포넌트도 함께 살림

2. **placeholder 페이지 → 실제 페이지로 변경 후 사장님이 다시 redirect로 되돌림 → AI가 또 실제 페이지로 변경**
   - 예: `/settlement`, `/reservations` — 사장님이 이미 redirect 로 되돌림
   - 원인: AI가 git diff 가 아닌 "현재 파일 상태"만 봄 → 사장님 의도 모름

3. **기능 카드 / 메뉴 / 모듈을 사장님이 비표시 처리했는데 다시 표시됨**
   - 예: 인스타 카드뉴스 / 플랫폼 통합 / 블로그 순위 promo strip
   - 원인: 새 기능 추가 작업 중 "관련 카드도 같이 노출시키자" 판단

4. **여신금융 / 홈택스 같은 미사용 플랫폼이 코드에 남아있음**
   - 사장님 요청으로 제거됨 (2026-05-06)

---

## 절대 다시 추가 / 변경하지 말 것

### A) UI 컴포넌트 / 위젯

| 항목 | 위치 | 결정일 | 이유 |
|------|------|--------|------|
| `QuickNav` (사이드바 옆 빠른 이동) | `app/dashboard/page.tsx` 내 `<QuickNav />` 호출 | 2026-04-28 / 2026-05-06 (재확인) | 사장님이 우측 사이드바 (`DashboardRightSidebar`) 로 대체. QuickNav 함수 정의는 남아있어도 호출 X |
| `QuickActions` 위젯 (대시보드 메인) | `app/dashboard/page.tsx` `<QuickActions />` | 2026-05-06 | 우측 사이드바로 이전됨 (`DashboardRightSidebar`) |
| 신규 모듈 promo strip 3종 (인스타 카드뉴스 / 플랫폼 통합 / 블로그 순위) | `app/dashboard/page.tsx` | 2026-05-06 | 사장님 요청으로 비표시. 우측 사이드바 빠른 액션에서 동일 기능 접근 |
| `/pricing` 좌측 사이드바 | `app/pricing/page.tsx` | 2026-05-06 | 공개 랜딩성 페이지로 단순화 (TopNav만 유지) |

### B) 플랫폼 / 데이터

| 항목 | 위치 | 결정일 | 이유 |
|------|------|--------|------|
| 여신금융 (`yeoshin`) | 전체 사이트 | 2026-05-06 | 사장님 요청 제거. `CANONICAL_PLATFORMS`, `PLATFORM_LABEL`, `PLATFORM_COLOR`, `INITIAL_PLATFORMS` 모두에서 제외 |
| 홈택스 (`hometax`) | 전체 사이트 | 2026-05-06 | 사장님 요청 제거. 동일 |
| `금융·세무` 카테고리 | `app/dashboard/page.tsx` Platform interface | 2026-05-06 | yeoshin/hometax 제거되면서 자동 제외 |

### C) 페이지 / 라우트

| 라우트 | 현재 상태 | 결정일 | 이유 |
|--------|-----------|--------|------|
| `/settlement` | redirect to `/dashboard` | 2026-05-06 (사장님 직접 되돌림) | 매출 캘린더는 시기상조. AI가 만든 캘린더 페이지를 사장님이 명시적으로 redirect 로 되돌렸음 |
| `/reservations` | 실제 페이지 (예약 관리) | 유지 | 사장님 사용 중 |
| `/marketing/events` | 실제 페이지 (이벤트 매니저) | 유지 | 사장님 사용 중 |
| `/marketing/instagram-feed` | 실제 페이지 | 유지 | 사장님 사용 중 |
| `/help` | 실제 페이지 (도움말) | 유지 | 사장님 사용 중 |

### D) 디자인 규칙 (CLAUDE.md 핵심)

- **이모지 사용 금지** (UI 코드 / 답변 / 문서 모두) — `→ ← · ✓ ★` 같은 기본 기호만 허용
- **L 단일 글자 로고 금지** — 무조건 `/public/logo-icon.svg` 사용 (TopNav / Sidebar / Login mobileTopBar)
- **bg color**: `#F8F9FA` 만 사용 (`#F2F4F6`, `#F8F9FB`, `#F9FAFB` 모두 표준 아님)
- **wrapper 패턴**: `<div min-h-screen bg-[#F8F9FA]><Sidebar /><div md:ml-[220px] flex flex-col min-h-screen><PageHeader /><main flex-1 px-4 md:px-6 py-4 md:py-6 max-w-6xl mx-auto w-full>...</main><Footer /></div></div>`
- **max-width 금지**: `max-w-[1400px]`, `max-w-[1200px]`, `max-w-7xl` (좌측 쓰림 발생)
- **모바일 상단 padding**: `pt-14`, `pt-16`, `pt-20` 금지 → `pt-4 md:pt-0` (PageHeader가 자체 처리)

### E) 라이브 통계 / 카운터 (login mobile)

- **누적 사장님**: 142명에서 시작, 단조 증가만 (절대 감소 X)
- **localStorage key**: `localution.live_stats_v1` — 항상 `Math.max(saved, computed)` 패턴으로 업데이트

---

## 새 기능 추가 작업 시 체크리스트

작업 시작 전:
- [ ] 이 문서 (`DO_NOT_TOUCH.md`) 전체 읽기
- [ ] `CLAUDE.md` 읽기 (디자인 규칙, 빠른 작업 매뉴얼)
- [ ] 사장님이 "이렇게 만들어달라"고 한 부분만 작업 — 추가 제안하지 말 것
- [ ] 기존 컴포넌트 / 페이지를 변경하기 전에 git log 한 번 확인

작업 완료 후:
- [ ] 추가한 컴포넌트가 이 문서의 "DO NOT" 목록과 충돌하지 않는지 확인
- [ ] 사장님 요청과 무관한 변경은 하지 않음 (예: "버튼 색만 바꿔줘" → 위치는 건드리지 말 것)
- [ ] 새로 추가한 기능이 이 문서에 추가될 만한 항목이면 즉시 등록

---

## 사장님 요청에 없는 작업은 하지 마세요

**잘못된 패턴**:
- "X 페이지 헤더 통일해줘" → 헤더만 바꾸지 않고 옆 카드들도 "관련 있어 보여서" 함께 정리

**올바른 패턴**:
- "X 페이지 헤더 통일해줘" → 헤더만 바꿈. 다른 부분은 사장님이 구체적으로 요청할 때만 작업

**자체 진행 작업 ("계속 진행해서 만들어나가")** 시에도:
- DO_NOT_TOUCH.md 와 충돌하지 않는 범위에서만 추가
- 새 페이지 / 기능을 만들 때는 기존 페이지를 변형하지 말고 신규 라우트로 추가
- 기존 페이지 수정 시에는 변경 범위를 명확히 한정 (예: 헤더만 / 데이터만 / 색상만)

---

## 변경 이력

| 일자 | 변경 | 결정자 |
|------|------|--------|
| 2026-05-06 | 초기 문서 작성 — 반복 회귀 패턴 정리 | AI ↔ 사장님 |
| 2026-05-06 | 여신금융·홈택스 전체 사이트 제거 | 사장님 |
| 2026-05-06 | QuickNav 재확인 — 다시 호출하지 말 것 | 사장님 |
| 2026-05-06 | 대시보드 promo strip 3종 비표시 | 사장님 |

---

## 이 문서 업데이트 규칙

- 사장님이 "이거 다시 되돌려줘 / 빼줘 / 보이지 않게 해줘" 라고 할 때마다 → 즉시 이 문서에 항목 추가
- AI가 의도치 않게 회귀를 일으켰을 때 → 원인 분석을 "왜 이 문서가 필요한가" 섹션에 보강
